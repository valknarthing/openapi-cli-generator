package main

import (
	"{{ .ModuleName }}/internal/cli"
)

func main() {
	cli.Init(&cli.Config{
		AppName:   "{{ .Name }}",
		EnvPrefix: "{{ .NameEnv }}",
		Version:   "1.0.0",
	})

	// TODO: Add register commands here.

	cli.Root.Execute()
}
